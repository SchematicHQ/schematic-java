/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.schematic.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ApiKeyScope {
    public static final ApiKeyScope CAPTURE = new ApiKeyScope(Value.CAPTURE, "capture");

    public static final ApiKeyScope WRITE = new ApiKeyScope(Value.WRITE, "write");

    public static final ApiKeyScope ADMIN = new ApiKeyScope(Value.ADMIN, "admin");

    public static final ApiKeyScope READ = new ApiKeyScope(Value.READ, "read");

    private final Value value;

    private final String string;

    ApiKeyScope(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof ApiKeyScope && this.string.equals(((ApiKeyScope) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case CAPTURE:
                return visitor.visitCapture();
            case WRITE:
                return visitor.visitWrite();
            case ADMIN:
                return visitor.visitAdmin();
            case READ:
                return visitor.visitRead();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ApiKeyScope valueOf(String value) {
        switch (value) {
            case "capture":
                return CAPTURE;
            case "write":
                return WRITE;
            case "admin":
                return ADMIN;
            case "read":
                return READ;
            default:
                return new ApiKeyScope(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ADMIN,

        CAPTURE,

        READ,

        WRITE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitAdmin();

        T visitCapture();

        T visitRead();

        T visitWrite();

        T visitUnknown(String unknownType);
    }
}
