/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.schematic.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class DataExportMetadata {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private DataExportMetadata(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static DataExportMetadata auditLog(AuditLogExportMetadata value) {
        return new DataExportMetadata(new AuditLogValue(value));
    }

    public static DataExportMetadata companyFeatureUsage(CompanyFeatureUsageExportMetadata value) {
        return new DataExportMetadata(new CompanyFeatureUsageValue(value));
    }

    public boolean isAuditLog() {
        return value instanceof AuditLogValue;
    }

    public boolean isCompanyFeatureUsage() {
        return value instanceof CompanyFeatureUsageValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<AuditLogExportMetadata> getAuditLog() {
        if (isAuditLog()) {
            return Optional.of(((AuditLogValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CompanyFeatureUsageExportMetadata> getCompanyFeatureUsage() {
        if (isCompanyFeatureUsage()) {
            return Optional.of(((CompanyFeatureUsageValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof DataExportMetadata && value.equals(((DataExportMetadata) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAuditLog(AuditLogExportMetadata auditLog);

        T visitCompanyFeatureUsage(CompanyFeatureUsageExportMetadata companyFeatureUsage);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "export_type",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(AuditLogValue.class), @JsonSubTypes.Type(CompanyFeatureUsageValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("audit-log")
    @JsonIgnoreProperties("export_type")
    private static final class AuditLogValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "export_type", allowSetters = true)
        private AuditLogExportMetadata value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AuditLogValue() {}

        private AuditLogValue(AuditLogExportMetadata value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAuditLog(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AuditLogValue && equalTo((AuditLogValue) other);
        }

        private boolean equalTo(AuditLogValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "DataExportMetadata{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("company-feature-usage")
    @JsonIgnoreProperties("export_type")
    private static final class CompanyFeatureUsageValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "export_type", allowSetters = true)
        private CompanyFeatureUsageExportMetadata value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CompanyFeatureUsageValue() {}

        private CompanyFeatureUsageValue(CompanyFeatureUsageExportMetadata value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCompanyFeatureUsage(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CompanyFeatureUsageValue && equalTo((CompanyFeatureUsageValue) other);
        }

        private boolean equalTo(CompanyFeatureUsageValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "DataExportMetadata{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("export_type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "DataExportMetadata{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
