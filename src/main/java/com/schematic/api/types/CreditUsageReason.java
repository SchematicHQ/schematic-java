/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.schematic.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CreditUsageReason {
    public static final CreditUsageReason LEASE_HOLD = new CreditUsageReason(Value.LEASE_HOLD, "lease_hold");

    public static final CreditUsageReason TRACK = new CreditUsageReason(Value.TRACK, "track");

    public static final CreditUsageReason MANUAL_ADJUSTMENT =
            new CreditUsageReason(Value.MANUAL_ADJUSTMENT, "manual_adjustment");

    public static final CreditUsageReason RESERVATION_RELEASE =
            new CreditUsageReason(Value.RESERVATION_RELEASE, "reservation_release");

    public static final CreditUsageReason LEASE_RELEASE = new CreditUsageReason(Value.LEASE_RELEASE, "lease_release");

    public static final CreditUsageReason RESERVATION_HOLD =
            new CreditUsageReason(Value.RESERVATION_HOLD, "reservation_hold");

    public static final CreditUsageReason RECONCILIATION =
            new CreditUsageReason(Value.RECONCILIATION, "reconciliation");

    private final Value value;

    private final String string;

    CreditUsageReason(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CreditUsageReason && this.string.equals(((CreditUsageReason) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case LEASE_HOLD:
                return visitor.visitLeaseHold();
            case TRACK:
                return visitor.visitTrack();
            case MANUAL_ADJUSTMENT:
                return visitor.visitManualAdjustment();
            case RESERVATION_RELEASE:
                return visitor.visitReservationRelease();
            case LEASE_RELEASE:
                return visitor.visitLeaseRelease();
            case RESERVATION_HOLD:
                return visitor.visitReservationHold();
            case RECONCILIATION:
                return visitor.visitReconciliation();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CreditUsageReason valueOf(String value) {
        switch (value) {
            case "lease_hold":
                return LEASE_HOLD;
            case "track":
                return TRACK;
            case "manual_adjustment":
                return MANUAL_ADJUSTMENT;
            case "reservation_release":
                return RESERVATION_RELEASE;
            case "lease_release":
                return LEASE_RELEASE;
            case "reservation_hold":
                return RESERVATION_HOLD;
            case "reconciliation":
                return RECONCILIATION;
            default:
                return new CreditUsageReason(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        LEASE_HOLD,

        LEASE_RELEASE,

        MANUAL_ADJUSTMENT,

        RECONCILIATION,

        RESERVATION_HOLD,

        RESERVATION_RELEASE,

        TRACK,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitLeaseHold();

        T visitLeaseRelease();

        T visitManualAdjustment();

        T visitReconciliation();

        T visitReservationHold();

        T visitReservationRelease();

        T visitTrack();

        T visitUnknown(String unknownType);
    }
}
