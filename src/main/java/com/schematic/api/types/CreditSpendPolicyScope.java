/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.schematic.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CreditSpendPolicyScope {
    public static final CreditSpendPolicyScope USER = new CreditSpendPolicyScope(Value.USER, "user");

    public static final CreditSpendPolicyScope COMPANY = new CreditSpendPolicyScope(Value.COMPANY, "company");

    public static final CreditSpendPolicyScope GROUP = new CreditSpendPolicyScope(Value.GROUP, "group");

    private final Value value;

    private final String string;

    CreditSpendPolicyScope(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CreditSpendPolicyScope
                        && this.string.equals(((CreditSpendPolicyScope) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case USER:
                return visitor.visitUser();
            case COMPANY:
                return visitor.visitCompany();
            case GROUP:
                return visitor.visitGroup();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CreditSpendPolicyScope valueOf(String value) {
        switch (value) {
            case "user":
                return USER;
            case "company":
                return COMPANY;
            case "group":
                return GROUP;
            default:
                return new CreditSpendPolicyScope(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        COMPANY,

        USER,

        GROUP,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCompany();

        T visitUser();

        T visitGroup();

        T visitUnknown(String unknownType);
    }
}
